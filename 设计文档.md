# AI 项目任务管理系统设计文档

## 1. 概述

当前项目为基于 React + Vite 的前端原型，页面展示了项目概览、任务列表、里程碑与通知预警。项目中所有数据均为前端静态定义。该设计文档基于当前页面数据结构推导出后端 API 输入/输出模型，并给出完整的接口设计方案。

## 2. 目标

- 设计可支持前端当前界面需求的 REST API。
- 定义项目、任务、里程碑、通知、统计等核心资源模型。
- 支持未来扩展 AI 预警、用户权限与多项目管理。

## 3. 系统架构

### 3.1 技术栈建议

- 前端：React 18 + Vite
- 后端：Node.js + Express / Koa / NestJS
- 数据库：MongoDB / PostgreSQL
- 鉴权：JWT 或 Session
- 监控：日志与错误处理

### 3.2 架构层次

- 前端展示层
  - `App.jsx` 渲染项目概况、任务卡片、里程碑与通知
- 后端服务层
  - REST API 负责项目、任务、里程碑、通知的 CRUD
  - 业务逻辑处理进度计算、预警规则、统计指标
- 数据层
  - 存储持久化项目与任务数据
  - 支持查询与筛选

## 4. 数据模型

### 4.1 Project

字段说明：
- `id`：项目唯一标识
- `name`：项目名称
- `department`：部门
- `status`：项目状态（未开始、进行中、延期、完成）
- `owner`：负责人
- `team`：成员信息或团队规模
- `start`：计划开始日期
- `end`：计划结束日期
- `milestone`：当前关键里程碑摘要
- `progress`：整体进度百分比
- `dependencies`：依赖说明

示例：
```json
{
  "id": "P-001",
  "name": "智能项目任务管理",
  "department": "产品与技术",
  "status": "进行中",
  "owner": "张经理",
  "team": "5 人",
  "start": "2026-06-01",
  "end": "2026-08-15",
  "milestone": "设计 → 开发 → 验收",
  "progress": 68,
  "dependencies": "需求评审、资源确认"
}
```

### 4.2 Task

字段说明：
- `id`：任务唯一标识
- `projectId`：所属项目 ID
- `name`：任务名称
- `owner`：负责人
- `status`：任务状态（已完成、进行中、未开始、阻塞）
- `progress`：任务完成率
- `planEnd`：计划结束日期
- `actualEnd`：实际完成日期
- `hours`：工时
- `comment`：备注

示例：
```json
{
  "id": "T-001",
  "projectId": "P-001",
  "name": "项目结构与需求定义",
  "owner": "李华",
  "status": "已完成",
  "progress": 100,
  "planEnd": "2026-06-08",
  "actualEnd": "2026-06-07",
  "hours": 28,
  "comment": "按时完成，需求明确。"
}
```

### 4.3 Milestone

字段说明：
- `id`：里程碑 ID
- `projectId`：所属项目 ID
- `title`：里程碑标题
- `date`：计划日期
- `status`：状态（完成、进行中、待开始）

示例：
```json
{
  "id": "M-001",
  "projectId": "P-001",
  "title": "需求确认",
  "date": "2026-06-05",
  "status": "完成"
}
```

### 4.4 Notification

字段说明：
- `id`：通知 ID
- `projectId`：所属项目 ID
- `title`：通知标题
- `time`：时间描述
- `level`：等级（提示、预警、正常）
- `type`：通知类型（提醒、风险、进度）
- `read`：已读状态

示例：
```json
{
  "id": "N-001",
  "projectId": "P-001",
  "title": "任务“原型设计与交互评审”未更新状态",
  "time": "2小时前",
  "level": "提示",
  "type": "risk",
  "read": false
}
```

### 4.5 DashboardSummary

字段说明：
- `projectStatus`：项目状态汇总
- `progress`：项目整体进度
- `taskCount`：任务总数
- `completedCount`：已完成任务数
- `pendingWarnings`：预警数量

示例：
```json
{
  "projectStatus": "进行中",
  "progress": 68,
  "taskCount": 3,
  "completedCount": 1,
  "pendingWarnings": 2
}
```

## 5. API 设计

### 5.1 响应格式

统一响应结构：
```json
{
  "success": true,
  "data": {},
  "message": "",
  "error": null
}
```

失败响应示例：
```json
{
  "success": false,
  "data": null,
  "message": "项目不存在",
  "error": {
    "code": 404,
    "details": "Project P-999 not found"
  }
}
```

### 5.2 项目接口

#### GET /api/projects

获取项目列表。

输入：无。

输出示例：
```json
{
  "success": true,
  "data": [
    {
      "id": "P-001",
      "name": "智能项目任务管理",
      "department": "产品与技术",
      "status": "进行中",
      "owner": "张经理",
      "team": "5 人",
      "start": "2026-06-01",
      "end": "2026-08-15",
      "milestone": "设计 → 开发 → 验收",
      "progress": 68,
      "dependencies": "需求评审、资源确认"
    }
  ],
  "message": "项目列表获取成功",
  "error": null
}
```

#### GET /api/projects/{projectId}

获取单个项目详情。

输入：URL 参数 `projectId`

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "P-001",
    "name": "智能项目任务管理",
    "department": "产品与技术",
    "status": "进行中",
    "owner": "张经理",
    "team": "5 人",
    "start": "2026-06-01",
    "end": "2026-08-15",
    "milestone": "设计 → 开发 → 验收",
    "progress": 68,
    "dependencies": "需求评审、资源确认"
  },
  "message": "项目详情获取成功",
  "error": null
}
```

#### POST /api/projects

创建新项目。

输入示例：
```json
{
  "name": "智能项目任务管理",
  "department": "产品与技术",
  "status": "进行中",
  "owner": "张经理",
  "team": "5 人",
  "start": "2026-06-01",
  "end": "2026-08-15",
  "milestone": "设计 → 开发 → 验收",
  "dependencies": "需求评审、资源确认"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "P-002",
    "name": "智能项目任务管理",
    "department": "产品与技术",
    "status": "进行中",
    "owner": "张经理",
    "team": "5 人",
    "start": "2026-06-01",
    "end": "2026-08-15",
    "milestone": "设计 → 开发 → 验收",
    "progress": 0,
    "dependencies": "需求评审、资源确认"
  },
  "message": "项目创建成功",
  "error": null
}
```

#### PUT /api/projects/{projectId}

更新项目。

输入示例：
```json
{
  "status": "延期",
  "progress": 55,
  "dependencies": "等待需求确认"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "P-001",
    "status": "延期",
    "progress": 55,
    "dependencies": "等待需求确认"
  },
  "message": "项目更新成功",
  "error": null
}
```

### 5.3 任务接口

#### GET /api/projects/{projectId}/tasks

获取指定项目的任务列表。

输入：URL 参数 `projectId`

输出示例：
```json
{
  "success": true,
  "data": [
    {
      "id": "T-001",
      "projectId": "P-001",
      "name": "项目结构与需求定义",
      "owner": "李华",
      "status": "已完成",
      "progress": 100,
      "planEnd": "2026-06-08",
      "actualEnd": "2026-06-07",
      "hours": 28,
      "comment": "按时完成，需求明确。"
    }
  ],
  "message": "任务列表获取成功",
  "error": null
}
```

#### GET /api/tasks/{taskId}

获取单个任务详情。

输入：URL 参数 `taskId`

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "T-001",
    "projectId": "P-001",
    "name": "项目结构与需求定义",
    "owner": "李华",
    "status": "已完成",
    "progress": 100,
    "planEnd": "2026-06-08",
    "actualEnd": "2026-06-07",
    "hours": 28,
    "comment": "按时完成，需求明确。"
  },
  "message": "任务详情获取成功",
  "error": null
}
```

#### POST /api/projects/{projectId}/tasks

创建任务。

输入示例：
```json
{
  "name": "原型设计与交互评审",
  "owner": "王敏",
  "status": "进行中",
  "progress": 72,
  "planEnd": "2026-06-18",
  "actualEnd": "",
  "hours": 36,
  "comment": "UI 细节待确认。"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "T-002",
    "projectId": "P-001",
    "name": "原型设计与交互评审",
    "owner": "王敏",
    "status": "进行中",
    "progress": 72,
    "planEnd": "2026-06-18",
    "actualEnd": "",
    "hours": 36,
    "comment": "UI 细节待确认。"
  },
  "message": "任务创建成功",
  "error": null
}
```

#### PUT /api/tasks/{taskId}

更新任务。

输入示例：
```json
{
  "status": "已完成",
  "progress": 100,
  "actualEnd": "2026-06-07",
  "hours": 30,
  "comment": "已完成，含验收修正。"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "T-002",
    "status": "已完成",
    "progress": 100,
    "actualEnd": "2026-06-07",
    "hours": 30,
    "comment": "已完成，含验收修正。"
  },
  "message": "任务更新成功",
  "error": null
}
```

#### DELETE /api/tasks/{taskId}

删除任务。

输出示例：
```json
{
  "success": true,
  "data": null,
  "message": "任务删除成功",
  "error": null
}
```

### 5.4 里程碑接口

#### GET /api/projects/{projectId}/milestones

获取项目里程碑列表。

输出示例：
```json
{
  "success": true,
  "data": [
    {
      "id": "M-001",
      "projectId": "P-001",
      "title": "需求确认",
      "date": "2026-06-05",
      "status": "完成"
    }
  ],
  "message": "里程碑列表获取成功",
  "error": null
}
```

#### POST /api/projects/{projectId}/milestones

创建里程碑。

输入示例：
```json
{
  "title": "开发启动",
  "date": "2026-06-20",
  "status": "待开始"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "M-003",
    "projectId": "P-001",
    "title": "开发启动",
    "date": "2026-06-20",
    "status": "待开始"
  },
  "message": "里程碑创建成功",
  "error": null
}
```

#### PUT /api/milestones/{milestoneId}

更新里程碑。

输入示例：
```json
{
  "status": "进行中"
}
```

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "M-002",
    "status": "进行中"
  },
  "message": "里程碑更新成功",
  "error": null
}
```

### 5.5 通知接口

#### GET /api/projects/{projectId}/notifications

获取项目通知列表。

输出示例：
```json
{
  "success": true,
  "data": [
    {
      "id": "N-001",
      "projectId": "P-001",
      "title": "任务“原型设计与交互评审”未更新状态",
      "time": "2小时前",
      "level": "提示",
      "type": "risk",
      "read": false
    }
  ],
  "message": "通知列表获取成功",
  "error": null
}
```

#### PUT /api/notifications/{notificationId}/read

将通知标记为已读。

输出示例：
```json
{
  "success": true,
  "data": {
    "id": "N-001",
    "read": true
  },
  "message": "通知已读",
  "error": null
}
```

### 5.6 仪表盘与统计接口

#### GET /api/projects/{projectId}/summary

获取项目仪表盘摘要。

输出示例：
```json
{
  "success": true,
  "data": {
    "projectStatus": "进行中",
    "progress": 68,
    "taskCount": 3,
    "completedCount": 1,
    "pendingWarnings": 2
  },
  "message": "项目摘要获取成功",
  "error": null
}
```

#### GET /api/reports/progress

获取整体进度报告或分析数据。

输出示例：
```json
{
  "success": true,
  "data": {
    "totalProjects": 5,
    "averageProgress": 72,
    "overdueTasks": 4,
    "riskTasks": 2
  },
  "message": "进度报告获取成功",
  "error": null
}
```

## 6. 安全与认证

- 建议启用 JWT 或 Session 认证。
- 所有写操作接口应校验用户身份与权限。
- 资源访问应根据项目成员角色授权。
- 通知与预警数据支持「仅项目成员可见」。

## 7. API 交互示例

### 7.1 首次加载项目页面

1. 前端请求 `GET /api/projects/P-001`
2. 请求 `GET /api/projects/P-001/tasks`
3. 请求 `GET /api/projects/P-001/milestones`
4. 请求 `GET /api/projects/P-001/notifications`
5. 请求 `GET /api/projects/P-001/summary`

### 7.2 更新任务进度

1. 前端发送 `PUT /api/tasks/T-002`
2. 后端更新任务状态与进度
3. 后端重新计算项目进度并触发预警规则
4. 若产生预警，则写入通知 `POST /api/projects/P-001/notifications`

## 8. 扩展设计建议

### 8.1 AI 预警与建议接口

建议增加以下接口：
- `GET /api/projects/{projectId}/risk-analysis`
- `GET /api/tasks/{taskId}/suggestions`
- `POST /api/ai/predict-delay`

输出示例：
```json
{
  "success": true,
  "data": {
    "riskScore": 0.78,
    "reason": "任务进度低于计划，依赖项未完成",
    "recommendation": "建议将“原型设计与交互评审”提前资源支持，并确认依赖项状态。"
  },
  "message": "风险分析成功",
  "error": null
}
```

### 8.2 多项目与权限扩展

- 增加 `GET /api/users/{userId}/projects`，按成员筛选项目列表。
- 增加 `GET /api/projects/{projectId}/members`，获取项目成员与角色信息。
- 增加 `GET /api/teams` 与 `GET /api/users`，支持组织结构管理。

## 9. 结语

本设计文档基于当前前端原型的数据结构与展示需求，提出了一套完整的 API 输入输出规范。该方案可直接用于后端开发对接，并支持后续引入 AI 预警、权限管理与多项目扩展。