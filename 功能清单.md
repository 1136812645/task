# AI 项目任务管理原型 功能清单

## 1. 项目概述

本项目是一个基于 React 与 Vite 的前端原型，用于展示“AI 项目任务管理”系统的可视化仪表盘和任务管理能力。当前实现为静态演示页面，数据在前端硬编码，未包含后端服务、持久化存储或真实 AI 模型。

## 2. 当前功能清单

### 2.1 项目基本信息展示
- 显示项目名称：`智能项目任务管理`
- 显示部门、状态、负责人、团队规模、计划周期、依赖项等项目元数据
- 显示当前项目阶段/里程碑：`设计 → 开发 → 验收`

### 2.2 项目进度与指标
- 显示项目整体状态与当前进度百分比（68%）
- 显示任务数量统计
- 显示完成任务数量与整体进度条
- 显示多项项目指标卡片（项目状态、当前进度、任务数量）

### 2.3 任务列表
- 列表展示任务条目，包括任务编号、名称、负责人、状态、计划完成时间
- 显示任务进度条与百分比
- 显示每个任务的工时与备注信息
- 当前示例任务：`T-001`、`T-002`、`T-003`

### 2.4 里程碑展示
- 列表展示项目里程碑名称、计划时间与状态
- 当前示例里程碑：`需求确认`、`原型评审`、`开发启动`

### 2.5 预警与通知
- 列表展示预警与通知条目
- 当前示例通知包括任务未更新、依赖待确认、整体进度提醒等
- 每条通知显示时间与等级（提示 / 预警 / 正常）

### 2.6 响应式 UI 与视觉风格
- 基于 CSS Grid 实现布局
- 支持桌面与窄屏布局
- 深色主题、玻璃质感面板、渐变进度条

## 3. 技术架构

### 3.1 前端框架
- React 18
- Vite 5
- React DOM
- `@vitejs/plugin-react`

### 3.2 项目结构
- `index.html`：入口 HTML
- `src/main.jsx`：应用启动入口，将 `App` 渲染到 `#root`
- `src/App.jsx`：主组件，包含页面结构、数据模型与组件函数
- `src/styles.css`：全局样式与组件样式
- `vite.config.js`：Vite 配置文件
- `package.json`：依赖与脚本定义

### 3.3 数据与组件设计
- 数据定义为前端静态常量：`project`、`tasks`、`milestones`、`notifications`
- 可复用组件：`ProgressBar`、`TaskCard`、`MilestoneCard`、`NotificationCard`
- 通过 `useMemo` 计算任务数量与已完成任务数
- 页面布局采用 `grid-layout` 实现仪表盘与侧边面板

### 3.4 目前未实现的关键模块
- 后端 API / 数据库存储
- 用户身份认证与权限管理
- AI 预警算法与预测模型
- 任务创建、编辑、删除、状态更新交互
- 多项目、多团队、权限角色管理
- 日志与历史记录存储

## 4. 部署说明

### 4.1 本地运行
1. 进入项目目录：
   ```bash
   cd d:\AI项目识别优化
   ```
2. 安装依赖：
   ```bash
   npm install
   ```
3. 启动开发服务器：
   ```bash
   npm run dev
   ```
4. 访问页面：
   打开浏览器并访问 `http://localhost:5173`

### 4.2 生产构建
1. 进入项目目录：
   ```bash
   cd d:\AI项目识别优化
   ```
2. 执行构建命令：
   ```bash
   npm run build
   ```
3. 构建输出目录为 `dist/`
4. 将 `dist/` 内容部署到静态站点托管服务，例如 GitHub Pages、Netlify、Vercel、Apache/Nginx 静态站点等

### 4.3 典型静态部署方式
- GitHub Pages
  - 在仓库根目录执行 `npm run build`
  - 将 `dist/` 目录内容发布到 `gh-pages` 分支或通过 GitHub Pages 设置部署
- Netlify 或 Vercel
  - 设置项目仓库并指定构建命令 `npm run build`
  - 发布目录设置为 `dist`
- 本地 Nginx/Apache
  - 将 `dist/` 目录内容复制到服务器静态目录
  - 确保服务器使用 `index.html` 作为入口

## 5. 建议与下一步优化方向
- 将前端静态数据替换为后端 API 接口
- 增加任务新建/编辑/删除表单与持久化功能
- 引入认证与角色权限管理
- 添加 AI 风险分析与预警引擎
- 增强图表可视化：甘特图、团队负载图、趋势图
- 支持多项目管理、项目筛选与搜索功能
- 兼容移动端触控与更完善响应式布局
